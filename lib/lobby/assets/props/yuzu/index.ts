import type { SpriteAsset } from "../../types";

/** 목욕하는 카피바라 머리에 얹는 유자 한 알. 온천 그림 속 펠트 유자를 잘라 썼다. 폭은 drawYuzu가 정한다 */
export default { id: "yuzu", category: "props", width: 0.33 } as const satisfies SpriteAsset;
