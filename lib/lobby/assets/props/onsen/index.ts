import type { SpriteAsset } from "../../types";

/** 이끼 돌 테두리 유자 온천. 폭은 온천 지름(SPRING_RADIUS 2.6 × 2) + 테두리 1.2. 김은 코드로 그린다 */
export default { id: "onsen", category: "props", width: 6.4 } as const satisfies SpriteAsset;
