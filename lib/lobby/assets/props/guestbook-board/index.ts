import type { SpriteAsset } from "../../types";

/** 방명록 게시판 (한 칸, 앞에서 Space로 방명록을 연다) */
export default { id: "guestbook-board", category: "props", width: 1.3, offsetY: 2 } as const satisfies SpriteAsset;
